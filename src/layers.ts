import { layer } from './lib/Layer';

export const Controller = layer('controller', 1);
export const Action = layer('action', 2);
export const Service = layer('service', 3);
export const Repository = layer('repository', 4);
